<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="clr-namespace:VMManager.UI.Views"
    xmlns:viewModels="clr-namespace:VMManager.UI.ViewModels">

    <!-- 🧠 DataTemplates for ViewModel-to-View Mapping -->
    <DataTemplate DataType="{x:Type viewModels:DashboardViewModel}">
        <views:DashboardView />
    </DataTemplate>

    <DataTemplate DataType="{x:Type viewModels:ScheduleViewModel}">
        <views:ScheduleView />
    </DataTemplate>

    <DataTemplate DataType="{x:Type viewModels:SettingsViewModel}">
        <views:SettingsView />
    </DataTemplate>

    <!-- 💡 Add future views here -->
    <!--
    <DataTemplate DataType="{x:Type viewModels:NewModuleViewModel}">
        <views:NewModuleView />
    </DataTemplate>
    -->
</ResourceDictionary>
